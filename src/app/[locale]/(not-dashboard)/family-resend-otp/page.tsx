import { FamilyResendOtp } from '@/components/screens';

import { Metadata } from "next";

export const metadata: Metadata = {
  title: `Famly Resend Verification Code`
}

const Page = () => <FamilyResendOtp />;

export default Page;
