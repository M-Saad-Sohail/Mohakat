import { FamilyVerifyOtp } from '@/components/screens';
import { Metadata } from "next";

export const metadata: Metadata = {
  title: ` Family Verification Code`
}

const Page = () => <FamilyVerifyOtp />;

export default Page;
